<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    
    <title>My Seed Collection</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="plantIdx containerMaxWidth flex">
        <div class="plantIdxHeading flex">
            <h1>My Seed Collection</h1>
            <a class="newBtn" href="/users/<%= user._id %>/plants/new">Add Plant</a>
        </div>
        <ul class="plantCardContainer flex">
            <% plants.forEach(plant => { %>
                <li class="card">
                    <div class="plantImg">
                        <% if (plant.plantType === 'herb') { %>
                            <img src="/images/herbs.jpg">
                        <% } else if (plant.plantType === 'veggie') { %>
                            <img src="/images/veggie.jpg">
                        <% } else if (plant.plantType === 'fruit') { %>
                            <img src="/images/fruit.jpg">
                        <% } else if (plant.plantType === 'flower') { %>
                            <img src="/images/flowers.jpg">
                        <% } else if (plant.plantType === 'other') { %>
                            <img src="/images/other.jpg">
                        <% }%>
                    </div>
                    <div class="plantText">
                        <h2>
                            <a href="/users/<%= user._id %>/plants/<%= plant._id %>"><%= plant.plantName %></a>
                        </h2>
                    </div>
                </li>
            <% }) %>
        </ul>
    </div>
</body>
</html>